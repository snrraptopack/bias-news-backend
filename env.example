# API Keys
GOOGLE_AI_KEY=your_google_ai_api_key_here
GNEWS_API_KEY=your_gnews_api_key_here

# Server Configuration
PORT=3001

# Optional: Database path (defaults to ./data/bias_news.db)
# DB_PATH=./data/bias_news.db 

# Optional: Use MongoDB instead of SQLite (if set, overrides SQLite)
# MONGODB_URI=mongodb+srv://user:pass@cluster.example.mongodb.net/biaslab
# MONGODB_DB_NAME=biaslab

# Optional: Enable full-page scraping enrichment before AI analysis (default true)
# FULL_CONTENT_SCRAPE=true